# Public URL of your site. Used in metadata, sitemap, robots, OG image, IndexNow.
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# IndexNow key (Bing, Yandex, Seznam). Generate: openssl rand -hex 16
# Also drop a public/<key>.txt with only the key inside, for ownership verification.
INDEXNOW_KEY=
